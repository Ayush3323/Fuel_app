rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development-safe rules to unblock account bootstrap and testing.
    // Tighten these before production launch.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
